# Настройка логина через Telegram и панели разработчика

Вся конфигурация — в `index.html`, блок `CONFIG` (в начале `<script>`):

```js
var CONFIG = {
  botUsername: "",        // username бота без @ (для входа в браузере)
  statsEndpoint: "",      // URL Google Apps Script Web App
  adminIds: [],           // Telegram ID с доступом к панели, напр. [123456789]
  devSecret: "baha-dev",  // запасной вход: ...#dev=baha-dev
};
```

Пока поля пустые — приложение работает как раньше, события пишутся только
локально (в браузере), а дашборд показывает статистику этого устройства.

---

## 1. Хранилище статистики (Google Apps Script + Таблица)

1. Создайте Google-таблицу.
2. **Расширения → Apps Script**, вставьте содержимое `apps-script.gs`, сохраните.
3. **Deploy → New deployment → Web app**:
   - *Execute as*: **Me**
   - *Who has access*: **Anyone**
4. Скопируйте URL вида `https://script.google.com/macros/s/XXXX/exec`.
5. Вставьте его в `CONFIG.statsEndpoint`.

События начнут падать в лист `events`, а дашборд — показывать агрегаты по всем
пользователям (вкладка «Глобально»).

## 2. Логин через Telegram — только через старт бота

Верификация **единственным путём**: пользователь открывает бота, нажимает
`Start`, бот записывает его `id` (он сразу появляется в дашборде, событие
`bot_start`) и присылает кнопку **«📈 Открыть квиз»**. Кнопка открывает
приложение как Mini App прямо внутри Telegram — `id` приходит автоматически,
без пароля и без Login Widget.

- В браузере сайт показывает экран «Открыть бота» со ссылкой `t.me/<bot>` и
  инструкцией из трёх шагов. Пройти дальше можно только через бота.
- `CONFIG.botUsername` (без `@`) нужен для этой ссылки.
- Вебхук `/start` и кнопку-меню включает `setupBot` — см. раздел «3-бис».

Login Widget убран намеренно: доступ выдаётся только тем, кто стартовал бота.

## 3-бис. Бот: вебхук `/start` и кнопка-меню Mini App

В Apps Script (тот же проект, что и статистика):

1. **Project Settings → Script Properties**, добавьте свойства:
   - `BOT_TOKEN` — токен бота от @BotFather (**не** кладите его в `index.html`).
   - `WEBAPP_URL` — `https://zevs2018123-cloud.github.io/Baha/`
   - `WEBHOOK_SECRET` — любая строка (необязательно, защищает вебхук).
   - `SEED_ADMINS` — `@Desanji` (первичный админ, см. ниже).
2. Сделайте **Deploy** (Web app, Anyone), если ещё не сделали.
3. В редакторе выберите функцию **`setupBot`** и нажмите **Run** — она
   зарегистрирует вебхук и поставит кнопку-меню (Mini App). В логах увидите
   ответы Telegram (`"ok":true`).

После этого `/start` боту отвечает ID, а кнопка-меню открывает приложение —
оба пути работают без веб-логина.

## 3. Доступ к панели разработчика

Доступ выдаётся **тремя способами** (любого совпадения достаточно):

1. **Кнопкой прямо в дашборде** (рекомендуется) — раздел «Пользователи»:
   у каждого вошедшего есть кнопка **«Админ»**, по нажатию доступ выдаётся и
   сохраняется на сервере (Apps Script), то есть действует для всех устройств.
   Повторное нажатие («Убрать») отзывает доступ. Список ID собирается
   автоматически — никого вручную выписывать не нужно.
2. **Первичный админ (bootstrap)** — задаётся в Apps Script один раз:
   *Project Settings → Script Properties* → свойство **`SEED_ADMINS`** со
   значением вашего юзернейма или ID, напр. `@Desanji` (можно несколько через
   запятую). Этот человек становится админом и может выдавать доступ другим.
3. **В коде** — `CONFIG.adminIds` / `CONFIG.adminUsernames` (постоянные админы):

```js
adminIds: [123456789],
adminUsernames: ["Desanji"],
```

Запасной вход (до настройки сервера или для отладки): открыть ссылку с хэшем
`#dev=baha-dev` (значение из `CONFIG.devSecret`). Рекомендуется сменить секрет.

**Как узнать свой ID:** войдите через Telegram — ID показан в чипе аккаунта на
старте (нажмите, чтобы скопировать) и в шапке дашборда. В разделе «Пользователи»
у каждой строки есть кнопка **«⧉ ID»** для копирования.

---

## Что попадает в воронку

Шаги дашборда (уникальные пользователи на каждом этапе):

`app_open → start_click → view_map → module_open → quiz_start →
module_complete → view_final → cta_fxpro`

Плюс разбивка по модулям: сколько открыли, сколько прошли, средний % правильных.

## Приватность

- В Mini App сохраняются публичные поля Telegram (id, username, имя). Токены и
  личные данные не собираются.
- Гостям (без логина) присваивается анонимный локальный id.
- Проверка прав на панель — клиентская, а выдача доступа на сервере доверяет
  ID/username, который прислал клиент (без проверки подписи Telegram). Для
  внутренней аналитики этого достаточно. Если потребуется строгая защита
  (например, платный доступ) — нужно верифицировать подпись Telegram-логина по
  токену бота на стороне Apps Script. Не храните в таблице ничего чувствительного.
